{% extends "./layout.peb" %}

{% block title %}
Новая инвестиция в проект
{% endblock %}

{% block content %}

{% if model.form.errors is not empty %}
<div class="grid-x grid-padding-x">
    <div class="medium-12 cell warning">
        <ul>
            {% for error in model.form.errors %}
            <li>{{ error }}</li>
            {% endfor %}
        </ul>
    </div>
</div>
{% endif %}

<h2>Новая инвестиция в проект</h2>
<form method="POST">
    <label>Проект
        <select required name="projectId">
            {% for project in model.projects %}
            <option value="{{ project.id }}">{{ project.name }}</option>
            {% endfor %}
        </select>
    </label>
    <label>ФИО инвестора
        <input required type="text" name="investorName" value="{{ model.form.fields["investorName"] | first }}">
    </label>
    <label>Email
        <input type="email" name="contactInfo" value="{{ model.form.fields["contactInfo"] | first }}">
    </label>
    <label>Сумма
        <input required type="number" min="0" name="amount" value="{{ model.form.fields["amount"] | first }}">
    </label>
    <input type="submit" value="Добавить" class="button">
</form>
{% endblock %}